Arts World Tanjore - Image UI update

The uploaded source material contains image-upload sections for:
- AdminCategories.jsx
- AdminCollections.jsx
- AdminProducts.jsx

The requested improvement is a polished, consistent image-upload UI while preserving
the existing uploadImage() backend/service behavior.

Important: the uploaded material is command-output snippets, not the complete source
files. Therefore this ZIP does not fabricate or overwrite your working page files.
Use the provided snippets as the basis for the image UI replacement after supplying
the complete three page files if a drop-in ZIP is required.
